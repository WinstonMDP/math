#import "cfg.typ": cfg
#show: cfg

= Rings
$(K, +, *)$ is a ring $:=$
+ $(K, +)$ is an abelian group.
+ $K$ is closed under $*$.
+ $a(b + c) = a b + a c$.
+ $(b + c)a = b a + c a$.

$|K| > 1 -> 0 != 1$.

$(a + b)^n = sum_(i = 0)^n binom(n, i) a^i b^(n - i)$ in a commutative ring.
