#import "cfg.typ": cfg
#show: cfg

= Semigroups
$*$ is an operation on a set $M :=$
+ $* in M^(M times M)$.
+ $M$ is closed under $*$.

$(M, *)$ is a semigroup $:= *$ is an associative operation on $M$.
