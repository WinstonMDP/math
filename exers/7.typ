#import "../cfg.typ": *
#show: cfg

$ "Prove that" "rng"(x |-> a^x) = RR_+ $
$"rng"(x |-> a^x) subset.eq RR_+$

I'm proving that $ex(x): a^x = r$

$ex(x): (sup c: a^c < r) <= x <= (inf c: r < a^c)$

$qed$
