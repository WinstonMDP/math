#import "cfg.typ": cfg
#show: cfg

= Algebras
$(V, +, *, dot)$ is an algebra over a field $F :=$
+ $(V, +, dot)$ is a vector space over $F$.
+ $(V, +, *)$ is a ring.
+ $(lambda a) b = a(lambda b) = lambda(a b)$.

$a b = sum_(i, j = 1)^n a_i b_j (e_i e_j)$.
